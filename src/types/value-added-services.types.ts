export interface TValueAddedService {
  id: string;
  name: string;
  duration: number; // in minutes
  estimatedMinutes: number;
  price: number;
  jobId: string;
  createdAt: string;
  updatedAt: string;
}
